{
  "name": "thai-chat-vercel",
  "version": "1.0.0",
  "type": "module",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "build": "cd client && npm install && npm run build",
    "vercel-build": "cd client && npm install && npm run build",
    "start": "echo 'Vercel deployment'",
    "dev": "cd client && npm run dev"
  },
  "dependencies": {
    "@vercel/node": "^5.3.6",
    "zod": "^3.24.2"
  },
  "devDependencies": {
    "typescript": "5.6.3"
  }
}